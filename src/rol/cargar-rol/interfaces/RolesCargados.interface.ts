export interface RolesCargadosProps {
	periodo: any;
}

export interface IRolCargado {
	id: number;
	modulo: number;
	nombre: string;
	fecha: string;
	// Agrega más campos según tu backend
}